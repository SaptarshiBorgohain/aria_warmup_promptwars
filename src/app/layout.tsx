import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ARIA — Adaptive Real-world Intelligence Assistant",
  description: "AI-powered emergency triage and incident management system. Multimodal input processing via Google Gemini for real-time P1/P2/P3 priority classification.",
  keywords: ["emergency triage", "AI triage", "medical AI", "Gemini AI", "disaster response"],
  authors: [{ name: "ARIA Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
